# 本地知识库系统需求文档

## 1. 项目概述
### 1.1 项目目标
构建一个基于Python的本地知识库系统，支持文档和代码的智能检索和问答。

### 1.2 技术栈
- **后端**: Python (FastAPI/Flask)
- **前端**: Python (Streamlit/Gradio) 或 Web框架
- **向量数据库**: Chroma/FAISS/Milvus
- **AI模型**: 本地开源模型 (如 ChatGLM, Qwen等)
- **文本嵌入**: Sentence-Transformers/BGE

## 2. 核心功能需求

### 2.1 文件管理
- **上传功能**
  - 支持单文件上传
  - 支持文件夹批量上传
  - 支持的文件类型:
    - 文档: .txt, .pdf, .docx, .md, .html
    - 代码: .py, .js, .java, .cpp, .go, .rs 等
  - 文件大小限制: 单文件不超过100MB

- **文件状态管理**
  - 显示已上传文件列表
  - 标识索引状态 (已索引/未索引/索引中/失败)
  - 显示文件元信息 (名称、大小、上传时间、类型)

- **删除功能**
  - 支持单个文件删除
  - 支持批量删除
  - 删除文件时自动清除对应的向量索引

### 2.2 索引管理
- **自动索引**
  - 文件上传后自动触发索引创建
  - 支持文档分块 (chunk_size: 500-1000字符)
  - 生成向量嵌入并存储

- **增量索引**
  - 仅对新上传文件建立索引
  - 已索引文件不重复处理
  - 支持索引更新机制

- **索引持久化**
  - 索引数据本地存储
  - 系统重启后自动加载已有索引

### 2.3 智能问答
- **查询功能**
  - 自然语言问答
  - 语义相似度检索
  - 支持多轮对话
  - 显示答案来源文件

- **AI模型集成**
  - 支持免费开源模型
  - 本地部署运行
  - 可配置模型参数 (温度、Top-k等)

- **检索优化**
  - 混合检索 (向量+关键词)
  - 结果重排序 (Rerank)
  - 相关度阈值过滤

## 3. 非功能需求

### 3.1 性能要求
- 单次文件上传响应时间 < 5秒
- 索引创建速度 > 1000字符/秒
- 问答响应时间 < 10秒

### 3.2 用户体验
- 友好的Web界面
- 实时索引进度显示
- 错误提示清晰明确

### 3.3 可维护性
- 代码模块化设计
- 详细的日志记录
- 配置文件外部化

## 4. 部署需求

### 4.1 启动脚本
- **后端启动脚本** (`start_backend.bat` / `start_backend.sh`)
  - 自动检查依赖
  - 启动API服务
  - 端口配置: 默认8000

- **前端启动脚本** (`start_frontend.bat` / `start_frontend.sh`)
  - 自动检查依赖
  - 启动Web界面
  - 端口配置: 默认8501

- **一键启动脚本** (`start_all.bat` / `start_all.sh`)
  - 同时启动前后端服务
  - 自动打开浏览器

### 4.2 环境配置
- Python版本: 3.9+
- 依赖管理: requirements.txt
- 虚拟环境支持

## 5. 数据存储结构
```
knowledge-base/
├── data/                  # 上传文件存储
│   ├── documents/        # 文档文件
│   └── code/             # 代码文件
├── indexes/              # 向量索引
├── logs/                 # 日志文件
└── config/               # 配置文件
```

## 6. 开发计划
1. **Phase 1**: 基础框架搭建 (文件上传、存储)
2. **Phase 2**: 索引系统实现 (向量化、存储)
3. **Phase 3**: AI问答集成 (模型接入、检索)
4. **Phase 4**: 界面优化、部署脚本
5. **Phase 5**: 测试、优化、文档完善